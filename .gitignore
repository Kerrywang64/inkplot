__pycache__/
*.pyc
.DS_Store

# CLI output — ignore only at the repo root; never use an unprefixed glob
# (this was once written as sheet*.png, which also blocked assets/sheet.png and broke the CI commit)
/art.json
/*.json.tmp
/sheet.png
/sheet-*.png
/sample.png
/gallery.html
/index.html
/tokens.css
/test.html

# Showcase images under assets/ must be committed; they are produced by scripts/make_assets.py
