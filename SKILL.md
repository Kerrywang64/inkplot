---
name: riso-press
description: 数据可视化 × 孔版印刷的配图生成器，外加一套纸感编辑风的设计决策库。当用户要做「编辑/杂志/出版/作品集/文档站/技术博客」风格的界面，需要「抽象配图 / 版画质感 / 数据图形装饰 / 占位图」但不想调生图模型，问「这个配色对比度够不够 / 用哪对字体 / 间距动效多少合适」，要一份设计规范或反模式清单，或者想给自己的图形产出做数值自检时使用。含 43 种数据可视化标本、7 种色场分割、6 种版式骨架、实测对比度色板、30+ 条反模式，以及两个量化自检脚本。
license: MIT
---

# riso-press

三层东西，配套使用。

**一、配图生成层** —— 数据可视化标本 × 色场分割 × 孔版质感。不调任何生图模型，不依赖任何素材文件，图像由 Canvas 逐层绘制。同种子完全可复现，权属干净。

**二、设计决策层** —— 已经做好的判断，读完直接照抄。版式骨架、字体配对、实测对比度的色板、间距与动效数值、一份反模式清单。

**三、自检层** —— 两个测量脚本，把「看着不对」变成可以对照的数。

---

## 设计理念

### 一 · 图形要有所指

抽象图案的问题不是不好看，是**抽出任何一张都联想不出意义**。所以这里的每张配图中心都是一个**数据图形**——饼图、注意力矩阵、持久图、沃罗诺伊。它自带语义：看到就知道在说分布、说注意力、说拓扑、说邻域。

不画装饰纹样，画信息的形状。

### 二 · 减法优先于加法

一张画面：**一个底色 + 一个第二色场 + 一个标本**，三件封顶。想加东西的时候先问能不能减。

密集不等于丰富。堆到第四件，画面立刻从"克制"掉进"杂乱"，而且所有图会开始长得一样——因为堆料掩盖了各自的结构差异。

### 三 · 变化来自实例，不来自数量

同一个标本出现十次却长十个样，比十个不同标本各出现一次更耐看。所以每个实例都过一层变换：旋转、镜像、缩放到出血裁切、五种笔法模式。

**加母题只降低重复的频率，不解决重复本身。** 这是本项目最贵的一课。

### 四 · 对比度是硬约束，不是审美偏好

第二色场与主色场的明度差**必须 ≥ 62**，否则两块颜色平分画面会互相打架。这条写死在代码里，不给随机数留余地。

同理，正文用色必须过 4.5:1。陶土 `#C66042` 在纸底上只有 3.53:1、白字在它上面 4.08:1——所以它永远不能做正文链接或按钮底。这不是口味问题，是量出来的。

### 五 · 敢用颜色，但只敢一次

色池分六族：土、绿、青蓝、紫粉红、高饱和跳色、中性。策略是**族内取主色，34% 概率跨族跳一次**。

保守的做法是永远在同一族里打转；莽撞的做法是每张都双跳色。正确的做法是敢跳，但一张只跳一次。

### 六 · 能测量的就不要猜

「看着不对但说不上哪不对」是最浪费时间的状态。所以带了两个脚本，把感觉变成数：

- `measure.mjs` —— 笔宽与墨覆盖。笔宽用**面积/边缘比**估计（`w ≈ 2×面积 ÷ 边缘像素数`），抗抗锯齿、抗压缩噪点，比游程众数可靠得多
- `diversity.mjs` —— 把每张降到 24×24 去均值，算两两 L2 距离。**只优化墨覆盖会把多样性优化没**，两个数必须一起盯

它们还能抓到肉眼看不见的错：圆角把采样点切成透明导致阈值反转、出血构图让边缘落在前景上。

### 七 · 可复现优先于惊喜

同种子 + 同参数 = 完全一致的输出。不接受"再跑一次可能更好"这种工作方式——那不是设计，是抽奖。

---

## 快速开始

```html
<script src="scripts/collage.js"></script>
<script>
  COLLAGE.init({ count: 24, seed: 20260808 });
  document.querySelectorAll('[data-plate]').forEach((el, i) => COLLAGE.attach(el, i));
</script>
```

自检：

```bash
node measure.mjs      # 笔宽 / 墨覆盖，对照参考基线
node diversity.mjs    # 两两结构距离，防重复
```

## 43 种标本

**分布** 直方图 · 点棒图 · 箱线图 · 小提琴图 · 蜂群图 · 山脊图 · 单位图
**比例** 饼图 · 环图 · 玫瑰图 · 矩形树图 · 圆填充 · 文氏图
**关系** 网络图 · 树图 · 聚类树 · 弦图 · 桑基图 · 弧连接图 · 矩阵图 · 平行坐标
**趋势** 折线 · 面积图 · 流图 · 训练曲线 · 名次流 · 斜率图 · 时间轴 · 螺线
**空间** 散点 · 等值线 · 六角分箱 · 沃罗诺伊 · 向量场 · 星云图 · 日历热图
**现代** 嵌入投影 · 注意力矩阵 · 候选分布 · 持久图 · 雷达图 · 三元图 · 仪表

## 7 种色场分割

`diagonal` 对角 · `vsplit` 竖分 · `hsplit` 横分 · `quad` 四分 · `inset` 内嵌 · `scallop` 扇贝边 · `band` 横带

## 参数

| 参数 | 说明 |
|---|---|
| `count` | 生成数量 |
| `seed` | 随机种子。同种子同参数 → 完全一致 |
| `scale` | 渲染倍率，默认跟随 `devicePixelRatio` |

`COLLAGE.meta(i)` 返回 `{skeleton, viz, zh, en, screened}`，可以直接拿来做图注。

## 也支持外部素材

```js
COLLAGE.materials([{ img: someImageElement }, ...]);
```

传了就用素材顶掉标本，色场分割、撕边、网点叠印照旧。**素材需自备**——挑选判据：去背或白底、单个物件而非场景、黑白或极低饱和（颜色交给色场，素材只提供形状与肌理）。

## 三个文件的分工

| 文件 | 装什么 | 什么时候读 |
|---|---|---|
| `references/design-system.md` | 色板 token（带实测对比度）、字体配对、字级间距刻度、动效规格、6 种版式骨架、交付自查表 | 动手之前 |
| `references/anti-patterns.md` | 30+ 条禁令，每条 `症状 → 为什么错 → 改法` | 动手之前 + 交付之前 |
| `scripts/collage.js` | 配图引擎 | 需要配图时 |

## 版权

图案手法（半调网点、撕纸边、干笔、套印错位）属印刷工艺传统，公共领域。数据可视化的图形语法同属公共领域。生成的图像由本地算法产生，无训练数据来源问题，可自由商用。本 skill 采用 MIT 协议。

若要做公开产品，仍应使用自己的品牌名、标识与文案。
