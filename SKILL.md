---
name: riso-press
description: 纸感编辑风的完整设计决策库 + 版画质感配图生成器。当用户要做「编辑/杂志/出版/作品集/文档站」风格的界面，要「纸感暖底 + 衬线排版」，问「这个配色对比度够不够 / 用哪对字体 / 间距动效多少合适」，要一份设计规范或反模式清单，或需要「抽象配图 / riso 孔版 / 半调 / 版画质感 / 占位图」但不想调生图模型时使用。含 6 种版式骨架、4 组字体配对、实测对比度的色板、间距字级动效 token、30+ 条反模式，以及 10 种图案结构的配图生成器。
license: MIT
---

# riso-press

两层东西，配套使用：

**一、设计决策层** —— 已经做好的判断，agent 读完直接照抄，不需要再权衡。版式骨架、字体配对、实测对比度的色板、间距与动效数值、以及一份反模式清单。

**二、配图生成层** —— 用算法印制版画质感的抽象配图。不调用任何生图模型，图像由 Pillow 逐层绘制、遮罩、套印、加颗粒生成。可复现、零成本、权属干净。

只覆盖**一种审美**：纸感编辑风。不做几百个行业的泛泛建议，把一种风格做到能直接交付。

## 动手前必读

按顺序做决定，**不要跳步**：

1. 读 `references/design-system.md` 开头 —— **先判断这套风格适不适用**。不适用就直说，换方案。
2. 选骨架（6 种，不要自创）→ 选字体配对（4 组）→ 定强调色（查对比度表，一页一个）
3. 套 token：间距、字级、动效时长直接抄，**不要自己发明数值**
4. 需要配图时跑 `scripts/generate.py`
5. 过 `design-system.md` 第八节自查表 + `references/anti-patterns.md` 逐条确认

第 5 步不能省。写完不检查的产出，跟默认输出的差距会在细节上被抹平。

## 文件分工

| 文件 | 装什么 | 什么时候读 |
|---|---|---|
| `references/design-system.md` | 色板 token（带实测对比度）、字体配对、字级间距刻度、动效规格、6 种版式骨架、组件规格、交付自查表 | 动手之前 |
| `references/anti-patterns.md` | 30+ 条禁令，每条「症状 → 为什么错 → 改法」；含本风格专属禁令与分场景禁令 | 动手之前 + 交付之前 |
| `references/patterns.md` · `palette.md` | 配图的图案结构与色板细节 | 需要生成配图时 |

## 快速开始

```bash
pip install pillow numpy

# 起始页：把设计决策变成能跑的代码
python3 scripts/scaffold.py --list                     # 看全部可选项
python3 scripts/scaffold.py --skeleton longform --out index.html
python3 scripts/scaffold.py --tokens-only --out tokens.css

# 配图：生成 24 幅，先看联系样张再决定要不要换种子
python3 scripts/generate.py --count 24 --seed 7 --contact sheet.png

# 画廊页
python3 scripts/gallery.py --art art.json --out gallery.html
```

`scaffold.py` 不是模板引擎，是把已经做好的判断打包成一个能跑的起点——token 全部内置，四种骨架，字体与强调色可选，对比度在生成时打印出来。

依赖只有 Pillow 与 numpy，无网络调用。

## 最容易违反的五条

完整清单在 `anti-patterns.md`，这五条最高频：

1. **陶土 `#C66042` 不能做正文链接或按钮底** —— 纸底上 3.53:1、白字 4.08:1，两个都不到 4.5。正文强调用铁锈 `#A33F2D`。
2. **不做深色模式** —— 这套的底层逻辑是模拟纸，纸没有深色模式。
3. **不用阴影堆层级** —— 用边框、留白、底色差。阴影只给真正浮起的东西（灯箱、下拉、toast）。
4. **不用胶囊圆角、不用 700 以上字重** —— 前者是 SaaS 语汇，后者在暖纸底上显脏。
5. **不删 `outline`** —— 删掉聚焦轮廓等于把键盘用户赶出门。

## 配图生成的三条纪律

**1 · 一幅一个主结构。** 底色 + 一个满幅主结构 + 最多一个经遮罩的副结构。堆到三层以上画面就变"杂"，所有图看起来像同一锅乱炖。遮罩把副层锁在约 40% 面积内。

**2 · 标题必须从图里长出来。** 标题 = `主色中文名 · 主结构中文名`，例如「赭石 · 涟漪」`Ochre Ripple`。随机诗意词配随机图，两边都失去意义。

**3 · 大面积永远是中性或单色。** 底色要么是纸色 `#F3EEE5`，要么是与主色明度差达标的同族色。

## 配图参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `--count` | 24 | 生成数量 |
| `--size` | 560 | 输出边长 px（内部固定 900 渲染后降采样） |
| `--colors` | 40 | 量化色数。降到 24 体积减半，更像真版画 |
| `--seed` | 随机 | **同种子同参数 → 完全一致的结果** |
| `--palette` | all | `all` / `warm`（陶土赭石系）/ `cool`（青蓝橄榄系） |
| `--pattern` | 随机 | 锁定单一结构，做同系列时用 |
| `--density` | 5 | 1–10，副结构出现概率。低=极简单层，高=层次密集 |
| `--contrast` | 5 | 1–10，底与主色明度差下限。低=柔和，高=强烈 |
| `--texture` | 5 | 1–10，套印错位与颗粒强度。低=干净，高=年代感 |
| `--contact` | — | 输出联系样张 PNG，**先看这个再决定要不要重生成** |

出图"太杂"就调低 `--density`；风格不统一就锁 `--palette`；要干净矢量感就调低 `--texture`。

```bash
python3 scripts/generate.py --count 12 --density 2 --contrast 4 --texture 3 --palette cool   # 极简
python3 scripts/generate.py --count 12 --density 8 --contrast 9 --texture 9 --palette warm   # 做旧
```

## 图案结构（10）

`halftone` 网点 · `hatch` 排线 · `rings` 涟漪 · `scatter` 散点 · `waves` 波纹
`grid` 网格 · `bars` 竖条 · `block` 块面 · `trace` 线迹 · `horizon` 地平

搭配经验、遮罩原理与后处理顺序见 `references/patterns.md`。

## 色板（18）

暖：陶土 铁锈 珊瑚 砖红 赭石 沙 酒红
冷：鼠尾草 橄榄 松林 苔绿 青碧 石板 靛蓝 天青 藏青 梅紫 丁香

纸 `#F3EEE5` · 墨 `#1A1815`。完整色值与 5 条配色硬规则见 `references/palette.md`。

## 扩展

**加图案**：在 `generate.py` 的 `PATTERNS` 里注册 `(函数, 中文名, 英文名)`，签名 `fn(draw, color, rng)`，在 900×900 画布作画。命名系统与画廊过滤条会自动接上。

**加颜色**：在 `PAL` 里加 `"key": ((r,g,b), "中文名", "English", "warm"|"cool")`。选色检查：转灰度后明度应落在 25%–75%。

自检两条：纸色底与深色底各跑一次都要成立；缩到 240px 看联系样张，缩小后仍能辨识的才算合格结构。

## 版权说明

图案手法（半调、排线、套印错位、撕纸遮罩）是印刷工艺传统，属公共领域。生成的图像由本地算法产生，无训练数据来源问题，可自由商用。本 skill 采用 MIT 协议。

暖纸底 + 有限色板 + 衬线排版这套视觉语言同样是通用编辑设计传统。但做公开产品时请使用自己的品牌名、标识与文案。
