---
name: riso-press
description: 程序化生成版画/riso 孔版质感的拼贴艺术图，并装配成编辑风格的画廊页。当用户要「生成艺术图/抽象配图/占位图」「riso/孔版/丝网/半调/版画质感」「编辑风格画廊/作品集页」「纸感暖底 + 衬线排版的网站」，或需要一批风格统一但互不重复的视觉素材（无需生图模型）时使用。含 10 种图案结构、18 色板、套印错位与颗粒后处理、可运行的画廊模板。
license: MIT
---

# riso-press

用算法印制版画。**不调用任何生图模型**——所有图像由 Pillow 逐层绘制、遮罩、套印、加颗粒生成。适合需要成批风格统一的抽象配图，但没有图像生成能力、或不想引入版权不明素材的场合。

## 什么时候用

- 需要 20–100 张风格统一、彼此不重复的抽象配图
- 做编辑/杂志/作品集风格的网站，需要纸感暖底的视觉语言
- 需要占位图，但不想用灰色方块或来源不明的图库图
- 想要「riso 孔版 / 丝网 / 半调 / 版画」质感

**不适用**：需要写实照片、人物、3D 渲染、具体物体的场景。这套只产出抽象构成。

## 快速开始

```bash
# 1. 生成 24 幅（含联系样张预览）
python3 scripts/generate.py --count 24 --seed 7 --contact sheet.png

# 2. 装配成画廊页
python3 scripts/gallery.py --art art.json --out gallery.html
```

依赖：`pip install pillow numpy`。仅用标准图像库，无网络调用。

## 三条设计纪律

这套东西容易做砸，砸法固定就那几种。以下三条是硬约束，改代码时不要破坏：

**1. 一幅一个主结构。** 每张图 = 底色 + 一个满幅主结构 + 最多一个经遮罩的副结构。堆到三层以上，画面立刻变成「杂」——所有图看起来都像同一锅乱炖，这是最常见的失败。副结构出现概率 50%，且必须被遮罩限制在约 40% 面积内。

**2. 标题必须从图里长出来。** 命名不是随机词库。标题 = `主色中文名 · 主结构中文名`，例如「赭石 · 涟漪」`Ochre Ripple`。看到名字就知道那张图长什么样，看到图就能猜到名字。随机诗意词库配随机图 = 两边都失去意义。

**3. 大面积永远是中性或单色。** 底色要么是纸色 `#F3EEE5`，要么是一个与主色明度差 ≥ 110 的同族色。不要让两个高饱和色平分画面。

## 参数

`scripts/generate.py`

| 参数 | 说明 |
|---|---|
| `--count` | 生成数量，默认 24 |
| `--size` | 输出边长 px，默认 560（内部始终 900 渲染后降采样） |
| `--colors` | 量化色数，默认 40。降到 24 体积减半，更像真版画 |
| `--seed` | 随机种子。同种子同参数 → 完全一致的结果 |
| `--palette` | `all` / `warm`（陶土赭石系）/ `cool`（青蓝橄榄系） |
| `--pattern` | 锁定单一结构，做同系列时用 |
| `--out` | 输出 JSON，默认 `art.json` |
| `--contact` | 额外输出联系样张 PNG，**先看这个再决定要不要重生成** |

`scripts/gallery.py`

| 参数 | 说明 |
|---|---|
| `--art` | 输入 JSON |
| `--out` | 输出 HTML（自包含，图以 data-URI 内嵌） |
| `--title` | 大标题，支持 `<i>` 斜体标签 |
| `--sub` | 副标题 |
| `--brand` / `--edition` | 页眉品牌与刊次 |
| `--layout` | `mosaic` 错落 / `quad` 双栏 / `solo` 单幅 |

## 图案结构（10）

`halftone` 网点 · `hatch` 排线 · `rings` 涟漪 · `scatter` 散点 · `waves` 波纹
`grid` 网格 · `bars` 竖条 · `block` 块面 · `trace` 线迹 · `horizon` 地平

## 色板（18）

暖：陶土 铁锈 珊瑚 砖红 赭石 沙 酒红
冷：鼠尾草 橄榄 松林 苔绿 青碧 石板 靛蓝 天青 藏青 梅紫 丁香

纸 `#F3EEE5` · 墨 `#1A1815`。详见 `references/palette.md`。

## 画廊页特性

- 三种排布实时切换（错落 / 双栏 / 单幅）
- 结构过滤条**由实际生成的图自动推导**，不是硬编码
- 灯箱：点开放大，← → 翻页，Esc 关闭，图注显示主/副结构与色数
- 纸张噪声叠层、滚动渐显、`prefers-reduced-motion` 降级
- 自包含单文件，无外部依赖（字体走 Google Fonts，断网降级到系统衬线）

## 体积控制

data-URI 内嵌会让 HTML 变大。经验值：

| 配置 | 单幅 | 36 幅总计 |
|---|---|---|
| `--size 560 --colors 40` | ~190 KB | ~6.8 MB |
| `--size 480 --colors 24` | ~90 KB | ~3.2 MB |
| `--size 400 --colors 16` | ~45 KB | ~1.6 MB |

超过 40 幅建议改成外链 PNG：修改 `gallery.py` 把 `src` 写成文件路径而不是 data-URI。

## 扩展

加新图案：在 `generate.py` 的 `PATTERNS` 字典里注册一个 `(函数, 中文名, 英文名)`，函数签名 `fn(draw, color, rng)`，在 900×900 画布上作画。命名系统会自动接上。

加新色：在 `PAL` 里加 `"key": ((r,g,b), "中文名", "English", "warm"|"cool")`。

## 版权说明

图案手法（半调、排线、套印错位、撕纸遮罩）是印刷工艺传统，属公共领域。生成的图像由本地算法产生，无训练数据来源问题，可自由商用。本 skill 采用 MIT 协议。

暖纸底 + 有限色板 + 衬线排版这套视觉语言同样是通用编辑设计传统（可追溯到中世纪印刷、Push Pin Studios、企鹅出版社）。但若要做公开产品，仍应使用自己的品牌名、标识与文案。
